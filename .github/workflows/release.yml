---
# Release Workflow - Calls Org-Level Reusable Workflow
# Handles automated releases with semantic versioning.
#
# Features:
#   - Pre-release testing
#   - Semantic versioning with conventional commits
#   - GitHub Release creation
name: Semantic Release

"on":
  workflow_run:
    workflows: ["CI"]
    types: [completed]
    branches:
      - main
      - master
  workflow_dispatch:
    inputs:
      force_release:
        description: 'Force release type (leave empty for auto)'
        required: false
        type: choice
        options:
          - ''
          - patch
          - minor
          - major
          - prerelease

# Prevent concurrent releases
concurrency:
  group: "release-${{ github.event.workflow_run.head_branch || github.ref_name }}"
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  release:
    name: Release Pipeline
    if: >-
      github.event_name == 'workflow_dispatch' ||
      github.event.workflow_run.conclusion == 'success'
    uses: ByronWilliamsCPA/.github/.github/workflows/python-release.yml@b7c661ec3bfeb04370ce3a9c748bd8c4e93bfe89  # main
    permissions:
      contents: write
      issues: write
      pull-requests: write
      id-token: write
      attestations: write
    with:
      python-version: '3.12'
      coverage-threshold: 80
      source-directory: 'src'
      semantic-release: true
      force-release: "${{ github.event.inputs.force_release || '' }}"
      publish-to-pypi: false
      pypi-package-name: 'fragrance-rater'
      # workflow_run fires only after CI passed, so tests are already
      # validated; a manual workflow_dispatch has no such upstream gate, so
      # run tests in the release pipeline to avoid releasing unvalidated code.
      run-tests: ${{ github.event_name == 'workflow_dispatch' }}
      skip-tests-reason: 'Tests already validated by upstream CI before workflow_run fires'
      # python-release.yml declares no `secrets:` in its workflow_call
      # block and references no secrets.* in any step; `secrets: inherit`
      # was unconditionally exposing every org/repo secret to a workflow
      # that uses none of them (CWE-200). Omitted entirely rather than
      # passing an empty block, since the reusable workflow accepts no
      # named secrets.
