---
title: "P1 Calibration Release Readiness Gate"
schema_type: common
status: published
owner: core-maintainer
purpose: "Repository readiness and deployment evidence status for P1."
tags:
  - planning
  - deployment
  - validation
---

**Status:** Repository controls complete; external deployment evidence required

**Owner:** Core maintainer

**Procedure:** [P1 release-readiness runbook](../../deployment/p1-release-readiness.md)

| Package | Repository deliverable | External evidence needed | Status |
| :--- | :--- | :--- | :--- |
| P1.1 | Strict manifest validator and blank template; [baseline/holdout source content](../evidence/baseline-v3.1-universal-and-holdout.md) received and versioned | Reviewed exact-version manifest and physical inventory | Blocked on inventory |
| P1.2 | Restore and inventory procedure | Redacted PostgreSQL 16 restore log and pre-upgrade inventory | Blocked on backup |
| P1.3 | Full chain and P2 head pass on fresh PostgreSQL 16 | Clone migration log, preservation assertions, plans, and schema hash | Fresh-schema pass; blocked on clone |
| P1.4 | Concurrency scenarios defined | PostgreSQL concurrency results at representative cardinality | Blocked on clone |
| P1.5 | Restore-based recovery procedure | Timed second restore and recovery checklist | Blocked on backup |
| P1.6 | No production host ports; Traefik/Authenik routing validator | Target-host reachability, bypass, and role tests | Awaiting deployment |
| P1.7 | Disclosure surfaces and forbidden fields enumerated | Redacted participant/admin disclosure matrix | Awaiting deployment |
| P1.8 | Python 3.12 tests, PostgreSQL integration, and remote CI pass | Target-host smoke report | CI pass; target pending |
| P1.9 | Parser tests use synthetic HTML | Authorized representative fixture provenance and coverage | Complete |
| P1.10 | Operations and failure procedure documented | Alert, backup, rotation, outage, and upgrade drill records | Awaiting deployment |

The gate remains open until every external evidence item has been reviewed. Repository tests,
SQLite results, synthetic HTML, and a locally rendered Compose file do not substitute for the
production clone and target topology.

## Local evidence, 2026-09-11

- Docker Compose v5 rendered the production override with no published app, frontend, or database
  ports and no application source mounts; the topology validator passed.
- Alembic upgraded a disposable PostgreSQL 16 container from an empty database through every
  revision to the then-current head. The gate must be repeated at the final release commit.
- The PostgreSQL integration gate generated a measured run, excluded an assigned holdout,
  appended feedback revision 1, and calculated explicit-denominator metrics; one test passed.
- The disposable database contained one run, one impression, and one response revision before it
  was destroyed.
- The final reviewed backend suite passed 622 tests with one environment-gated PostgreSQL test
  skipped and 87.71% coverage. The PostgreSQL test passed separately against PostgreSQL 16.
- A fresh PostgreSQL 16 migration reached `f5c2d3e4a5b6`; the additive usage-accounting revision
  downgraded to `e4b1c2d3f4a5` and upgraded to head again successfully.
- Ruff, BasedPyright, configured Bandit, strict MkDocs, Markdownlint, and the live dependency audit
  passed. The dependency audit reported no known vulnerabilities.
- The frontend passed five tests, ESLint, and the production build.
- [PR #70](https://github.com/ByronWilliamsCPA/fragrance-rater/pull/70) passed its remote CI,
  Python 3.10-3.13 compatibility, security, container, dependency, documentation, contract, FIPS,
  license, and quality gates before merge commit `7f0884fb3fd5a5faa5178952ae4ef1f08792d010`.

This establishes fresh-schema compatibility. It does not establish production-data preservation,
backup restorability, concurrency behavior at real cardinality, or target-network isolation.

## P1.1 evidence, 2026-09-12

- The product owner supplied the "AUTHORITATIVE UNIVERSAL BASELINE — V3.1" (33 fragrances / 39
  presentations, 6 hidden repeats) and "AUTHORITATIVE VALIDATION HOLDOUT SET — V3.1" (10
  fragrances) source content. It is versioned at
  [baseline/holdout source content](../evidence/baseline-v3.1-universal-and-holdout.md), with the
  structural counts and hidden-repeat day-separation re-derived and checked against the source
  document (all pass).
- This is the verified *design* input `docs/calibration-v1.md` describes as missing ("Add the
  verified list through program setup/API; no catalog identities were guessed from fragrance
  names"), not the P1.1 manifest itself: no entry yet has a resolved `FragranceVersion`,
  `source_url`, or `verification_evidence`, and `physical_sample_confirmed` is true for none of
  them (fragrances are on order; only #16 and #33 are already-owned inventory). P1.1 stays
  **blocked on inventory** per `scripts/validate_calibration_manifest.py`'s required fields.

## P1.9 evidence, 2026-09-12

- Fixture provenance: two live, publicly reachable Parfumo perfume pages (Creed Aventus, Chanel
  Bleu de Chanel) were each fetched once via an ordinary HTTPS GET - the same access any visitor's
  browser performs - to confirm current markup structure. Neither page's content is stored,
  committed, or redistributed in this repository; every fixture in
  `tests/unit/test_services/test_parfumo_scraper.py` is hand-authored synthetic HTML using
  fictitious brand/fragrance names, modeling the confirmed CSS classes and `data-*` attributes
  (`.barfiller_element.rating-details[data-type]`, the "Also liked" sidebar, the `.p_con`
  concentration trigger). The module docstring records this provenance in place.
- Parser coverage added for the required categories:
  - **Requested metrics**: `_extract_metrics` reads the five per-dimension community ratings
    (scent, longevity, sillage, bottle, value for money) from their scoped score/count elements.
  - **Status**: `_extract_production_status` reads Parfumo's own "still in production" / "no
    longer in production" sentence; anything unrecognized stays `None`.
  - **Similar fragrances**: `_extract_similar_fragrances` reads the sidebar "Also liked" list
    (real anchors); the in-page "Smells similar" widget is confirmed AJAX/JS-only
    (`data-p_id`/`data-s_id`, no `href`) and is not scraped.
  - **Related versions**: confirmed AJAX-only on the live site (`getConcentrationsPopup()`); no
    `related_versions` field was added, and a regression test
    (`TestParfumoScraperConcentrationVariantLimitation`) asserts none is fabricated, consistent
    with the "no guessed fragrance concentration or version" release-blocking invariant.
  - **Missing fields**: a fixture with no rating/description/similar sections confirms every new
    field defaults to empty/`None` rather than raising or guessing.
  - **Unknown concentration**: covered by the existing source-snapshot regression test; unchanged.
  - Along the way, fixture verification against the real nested rating-block markup found that
    `_extract_rating`'s prior whole-block-text regex could fuse the score and vote count into one
    bogus number on that markup shape; it now prefers the scoped score/count elements, with the
    original flat-text form kept as a fallback (both are regression-tested).
- 66 scraper tests and the full 617-test unit suite pass; Ruff, Ruff format, BasedPyright, and
  Bandit are clean on the changed files.
