# P0 Planning Baseline Gate

> **Status**: Ready for core-maintainer review
>
> **Audited implementation commit**: `d597e025798078e2ebfeb1f74615551aafb66ee7`
>
> **Planning branch**: `docs/p0-planning-baseline`
>
> **Date**: 2026-09-11

## Scope

P0 reconciled the project vision, authoritative plan, technical specification, roadmap,
controlled-calibration policy, contributor instructions, documentation navigation, and ADR
history. It compared those documents with the implemented catalog, ordinary encounter,
recommendation, source, authentication, calibration, and frontend code.

The complete finding-by-finding disposition is retained in the
[project plan](../PROJECT-PLAN.md#17-audit-finding-disposition).

## Planning decisions

- P1 and P2 are mandatory before D1.
- P1 separates merged calibration code from deployment verification.
- P2 adds the measurement path required to judge recommendation usefulness.
- The former 80% “interesting” target is a funnel metric rather than predictive accuracy.
- Actual post-sample liking, would-wear, and would-buy outcomes are evaluated separately.
- Code cannot silently override an accepted ADR.
- The historical Phase 0–4 roadmap is retired from execution.
- Milestone evidence is retained under a stable, reviewable project-gate record.

## Deliverables

- Project Vision v2
- Authoritative Project Plan v2 with current-state ledger and full audit disposition
- Technical Specification v2
- P0/P1/P2/D1–D5 execution roadmap
- ADR-005 through ADR-009 and supersession links from earlier ADRs
- Updated contributor planning instructions and architecture overview
- Published calibration, P0, and ADR navigation
- Template feedback for planning authority and validation gaps

## Validation

| Check | Result |
| :--- | :--- |
| Markdown lint across active planning documents | Passed |
| Front-matter validation for modified non-planning documentation | Passed |
| Strict MkDocs build after adding current navigation | Passed |
| Git whitespace/error check | Passed |
| Stale status phrase search | Passed after intentional historical/audit references were reviewed |
| Frontend tests used to verify current UI state | 3 passed |
| Frontend build and ESLint used to verify current UI state | Passed |

The strict build emits an upstream MkDocs ecosystem warning and lists intentionally unnavigated
reference/report pages. Neither condition is a broken planning link.

## P-sprint continuity record

P1/P2 implementation exposed repository-wide hook integration issues that were not visible in
the original planning-only validation. They were resolved before D1:

- the generic YAML hook now excludes files that use the supported MkDocs `!ENV` and Compose
  `!reset` tags, while Yamllint and the production-topology validator still check the Compose
  configuration;
- the strict JSON hook excludes TypeScript's JSONC configuration files;
- shebang-bearing maintenance scripts are executable; and
- the FIPS workflow's boolean dispatch input uses a schema-valid Boolean default.

The staged-file P1/P2 hook suite and the repository-wide hook suite both pass after these fixes.

## Deferred implementation evidence

A targeted recommendation-service test collected successfully but did not finish within 60
seconds under the local Python 3.14 environment. The project target for release verification is
Python 3.12 with PostgreSQL 16. P1.8 therefore requires deterministic target-environment results;
P0 does not claim backend release readiness.

The following are deliberately not P0 completion claims:

- live PostgreSQL migration and concurrency safety;
- production backup and restore;
- Authentik/Traefik backend-bypass prevention;
- full blind-disclosure coverage;
- exact 43-fragrance baseline verification; or
- recommendation feedback and outcome measurement.

Those items are assigned to P1 or P2 with explicit owners, acceptance criteria, and evidence.

## Review record

The core maintainer should confirm:

1. the vision states the intended product outcome;
2. P1 and P2 should block D1;
3. ADR-005 through ADR-009 reflect the approved policy;
4. the risk ownership and release-blocking invariants are acceptable; and
5. no audit finding is missing from the project-plan disposition table.

After review, update P0 to Complete in the authoritative plan and both roadmap mirrors.
