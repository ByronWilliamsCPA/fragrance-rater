# Docker Compose configuration for Fragrance Rater
# Development and production-ready setup with common services

services:
  # =============================================================================
  # Application Service
  # =============================================================================
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BUILD_ENV: ${ENVIRONMENT:-development}
    image: fragrance_rater:${VERSION:-latest}
    # No container_name: docker-compose.prod.yml scales this service via
    # deploy.replicas (default 2), and Compose requires a per-replica
    # generated name when replicas > 1. A static container_name here would
    # make production scaling fail to start at all.
    ports:
      - "${APP_PORT:-8000}:8000"
    environment:
      # Core settings
      ENVIRONMENT: ${ENVIRONMENT:-development}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      JSON_LOGS: ${JSON_LOGS:-false}

      # Database configuration.
      # No credential default: DB_PASSWORD must come from .env (see
      # .env.example) or the environment, so no weak literal password can
      # be inherited silently by any environment, including production.
      DATABASE_URL: ${DATABASE_URL:-postgresql+asyncpg://fragrance_rater:${DB_PASSWORD:?DB_PASSWORD must be set (copy .env.example to .env)}@db:5432/fragrance_rater}
      # Redis cache (if applicable)
      # REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}

      # Sentry (error tracking)
      # SENTRY_DSN: ${SENTRY_DSN:-}
      # SENTRY_ENVIRONMENT: ${ENVIRONMENT:-development}

      # Google Cloud credentials (if using Assured OSS)
      # GOOGLE_APPLICATION_CREDENTIALS_B64: ${GOOGLE_APPLICATION_CREDENTIALS_B64:-}
      # GOOGLE_CLOUD_PROJECT: ${GOOGLE_CLOUD_PROJECT:-}

    volumes:
      # Development: Mount source code for hot reload
      - ./src:/app/src:ro
      - ./tests:/app/tests:ro
      # Logs (optional)
      # - ./logs:/app/logs

    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health/live"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 40s
    restart: unless-stopped

    # Security: Run as non-root user
    user: "1000:1000"

    networks:
      - fragrance_rater-network

# =============================================================================
  # Frontend Service (React + Vite)
  # =============================================================================
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: development
    image: fragrance_rater-frontend:${VERSION:-latest}
    # No container_name: see the app service above; docker-compose.prod.yml
    # also sets deploy.replicas on this service (default 1, but the base
    # file must still not preclude scaling it).
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
    environment:
      - VITE_API_URL=http://localhost:${APP_PORT:-8000}
    volumes:
      # Development: Mount source code for hot reload
      - ./frontend/src:/app/src:ro
      - ./frontend/public:/app/public:ro
      - ./frontend/index.html:/app/index.html:ro
    depends_on:
      - app
    restart: unless-stopped

    networks:
      - fragrance_rater-network

# =============================================================================
  # Database Service (PostgreSQL)
  # =============================================================================
  db:
    image: postgres:16-alpine
    container_name: fragrance_rater-db
    environment:
      POSTGRES_DB: fragrance_rater
      POSTGRES_USER: fragrance_rater
      POSTGRES_PASSWORD: ${DB_PASSWORD:?DB_PASSWORD must be set (copy .env.example to .env)}
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=en_US.UTF-8"

    volumes:
      # Persistent data
      - postgres-data:/var/lib/postgresql/data
      # Init scripts (optional)
      # - ./scripts/init-db.sql:/docker-entrypoint-initdb.d/init.sql:ro

    # Bound to loopback by default: Compose merges list-valued fields like
    # `ports` by the composite key {ip, target, published, protocol}, so an
    # override in docker-compose.prod.yml only replaces this entry if its
    # ip matches. Publishing unbound here would let the prod override's own
    # 127.0.0.1 binding merge alongside this one instead of replacing it,
    # leaving the database reachable on all interfaces despite the prod
    # config. Run with DB_PORT unset (or override to 0.0.0.0 explicitly)
    # only on a host you trust the local network of.
    ports:
      - "127.0.0.1:${DB_PORT:-5432}:5432"

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fragrance_rater"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

    restart: unless-stopped

    networks:
      - fragrance_rater-network

# =============================================================================
  # Redis Service (Caching/Sessions)
  # =============================================================================
  # redis:
  #   image: redis:7-alpine
  #   container_name: fragrance_rater-redis
  #   command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD:-redispassword}
  #
  #   volumes:
  #     - redis-data:/data
  #
  #   ports:
  #     - "${REDIS_PORT:-6379}:6379"
  #
  #   healthcheck:
  #     test: ["CMD", "redis-cli", "--raw", "incr", "ping"]
  #     interval: 10s
  #     timeout: 3s
  #     retries: 5
  #
  #   restart: unless-stopped
  #
  #   networks:
  #     - fragrance_rater-network

# =============================================================================
# Volumes (Persistent Storage)
# =============================================================================
volumes:
  postgres-data:
    driver: local
  # redis-data:
  #   driver: local

# =============================================================================
# Networks
# =============================================================================
networks:
  fragrance_rater-network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.25.0.0/16

# =============================================================================
# Usage Instructions
# =============================================================================
# Development:
#   docker-compose up -d
#   docker-compose logs -f app
#   docker-compose exec app bash
#
# Production:
#   docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d
#
# Rebuild after dependency changes:
#   docker-compose up -d --build
#
# View logs:
#   docker-compose logs -f
#
# Stop services:
#   docker-compose down
#
# Clean up (including volumes):
#   docker-compose down -v
